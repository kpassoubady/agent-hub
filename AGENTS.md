# Agent Hub — AGENTS.md

This project uses `CLAUDE.md` as the canonical source of project context.

Read `CLAUDE.md` at the repo root for:
- Repository layout and what each directory contains
- Agent frontmatter schema and body structure
- Skill conventions and the loop-engine protocol
- `.agenthub-config.yaml` schema consumed by the agents
- What belongs in this hub vs. elsewhere
- Markdown formatting conventions
- Versioning and release process
